<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Artist Details</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
    <style>
      
      .container {
        
        position: relative;
        text-align: center;
        color: white;
      }
      .genre {
        
       text-align: center
        ;
        
      }
      ul {
        padding: 4px;
        margin: 0;
        /* width: 200px; */
      }

      li {
        padding: 2px;
      }
      .centered {
        position: absolute;
        top: 50%;
        left: 50%;
        color: rgb(255, 255, 255);
        font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
        font-size: 70px;
        transform: translate(-50%, -50%);
      }
    </style>
  </head>
  <body>
    <%- include('../navbar.ejs') %>
    
    <h2>Artist Details</h2>

    <section class="container">
      <img src="<%= resSongArtist.image %>" style="width: 50%" />
      <div class="centered" ><%= resSongArtist.name.charAt(0).toUpperCase() + resSongArtist.name.slice(1); %></div>
      
    </section>
    <br />

    <div class="genre">
      <h3>Genre : <%= resSongArtist.genre %></h3>
    </div>
    
    <section class="container">
      <table class="table table-bordered">
        <thead>
          <tr>
            <th scope="col">No.</th>
            <th scope="col">Song Title</th>
            <th scope="col">Duration</th>
            <th scope="col">Album</th>
            <th scope="col">Song Image</th>
          </tr>
        </thead>
        <tbody>
          <% resSongArtist.songs.forEach((e,index)=> { %>
          <tr>
            <th scope="row"><%= index +1 %></th>
            <td><%= e.name === null ? "-" : e.name %></td>
            <td><%= e.duration === null ? "0" : e.duration %> Min</td>
            <td><%= e.album === null ? "-" : e.album %></td>
            <% if(e.image === null || e.image === "" ) { %>
            <td>no image</td>
            <% } else { %>
            <td><img src="<%= e.image %>" height="100px" width="100px" /></td>
            <% } %> 
            
            <% }) %>
          </tr>
        </tbody>
      </table>
    </section>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
